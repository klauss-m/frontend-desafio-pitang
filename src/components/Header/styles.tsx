import { createStyles } from '@mantine/core';

const headerStyles = createStyles({
  header: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    height: '100%',
  },
});

export { headerStyles };
